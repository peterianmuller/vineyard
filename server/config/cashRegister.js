var cashRegister = function (priceList = [], currentFunds = 0) {
	//pricelist will be an array of item objects
	this.priceList = priceList;
	//transaction list will be a hash table that stores transaction objects
	this.moneyAvailable = currentfunds;
	this.transactions = [];
	this.transactionTotal = this.moneyAvailable + transactions
	this.transactionSearch = //maybe not a file store

	this.initializeRegister = 
	//if pricelist provided, will add pricelist
	//if current funds total is greater than zero, will set current funds to the input amount

}

//will also make a transaction constructor and line item constructor, so each time there is a new transaction we create a new transaction object

/*transaction constructor will be a queue*/

